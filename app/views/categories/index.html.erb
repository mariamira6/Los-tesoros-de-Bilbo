<div class="flex flex-col items-center">
  <h2 class="text-2xl font-bold mb-6 mt-6"><%= t(".title") %></h2>

  <div>
    <%= link_to t(".new"), new_category_path, class: "mt-4 bg-teal-600 text-white px-4 py-2 rounded hover:bg-teal-900 transition-colors duration-300 ease-in-out" %>
  </div>

  <div class="flex flex-wrap justify-center items-center gap-3">
    <%= render partial: "category", collection: @categories %>
  </div>
</div>

