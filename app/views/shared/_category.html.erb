<%= link_to products_path(category_id: category.id), class: "category w-auto font-bold text-white px-4 py-2 drop-shadow-sm transform transition-all duration-200 hover:bg-teal-700 hover:scale-105 hover:rounded-2xl" do %>
  <p><%= category.name %></p>
<% end %>
